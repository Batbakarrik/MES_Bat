import React, { useState } from 'react'

import { TextInput, StyleSheet, View, Text} from 'react-native'

    const Calcul1 = props => {
        const [coef_I, setCoef_I] = useState(0)
        // const calc_coef = () => setCoef_I(second_I / prim_I)
        return (
            <>
                {/* <Button onClick={Calcul1}>calc coef</Button> */}
            </>
        );
    };
    export default Calcul1